#include <iostream>

int main()
{
    // const keyword specifies that a variables value can't be changed
    // basically it makes the variable read-only

    // const DataType unique_name(conventionally all upper) = value(Based on Data Type);
    const double PI = 3.14;

    // if we were to run the below line it'll raise an error
    // PI = 420.69

    return 0;
}